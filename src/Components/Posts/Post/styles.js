import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    image: {
        height: 0,
        padding: '56.25%'

    },
    card: {
        height: '100%',
        borderRadius: '15px'
    },


}))

export default useStyles;